apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fakeprovider.fullname" . }}-test-connection"
  labels:
    {{- include "fakeprovider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fakeprovider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
